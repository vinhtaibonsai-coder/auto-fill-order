# GEMINI_CODE_REWRITE_PROGRESS.md

## Confirmed Surfaces and Roles
1. `index / panel`: Operational order workspace. Mainly used by staff. Primary entry point: `index.html` (main) and extension panel loaded via content scripts.
2. `options`: Shop control center. Mainly used by Owner/Manager. Primary entry point: `frontend/options/options.html`.
3. `admin-dashboard`: SaaS control plane. Mainly used by System Admin. Primary entry point: `admin-dashboard/admin.html` (admin).

## Active Entry Points and Configs
- **Vite entry points**: `index.html` (main page) and `admin-dashboard/admin.html` (admin page).
- **Extension content script files**: Defined in `manifest.json` under `content_scripts`, containing config files, supabase configs, domain auth/permissions services, address normalization engines, carrier autofill adapters, order-parser, and UI/panel loaders, leading to `src/runtime/content/index.js`.
- **Extension sync script**: `sync:ext` script in `package.json` copies manifest, frontend, and source files into the `extension` folder.

## Known Dirty Files (Git Status)
- This workspace had pre-existing dirty/generated changes before the later phases continued. No unrelated user changes were intentionally reverted.
- `extension/` is generated by `npm run sync:ext` and was re-synced after source changes.

## Completed Milestones
### Phase 1: Setup And Baseline (Completed)
- Surfaces confirmed: `index / panel`, `options`, `admin-dashboard`.
- Analyzed and verified all entry points in `vite.config.js`, `manifest.json`, and `package.json`.

### Phase 2: Foundational Risk Fixes (Completed)
- Gated mock fallback in `src/ui/panel/App.jsx` to localhost/127.0.0.1 development only. In production, panel gracefully degrades using local parsed data.
- Fixed undefined `shipFeeBox` in `src/runtime/content/index.js` by explicitly scoping and querying the checkbox element from DOM.
- Implemented real database mutation in `AdminRepository.updateShopPlan` and connected it to `AdminService.updateShopPlan`.
- Added strict authorization check in `AdminService._ensureAdmin` verifying `SYSTEM_ADMIN` role against `user_roles`.
- Synced all modifications to the `extension/` bundle.

### Phase 3: Tests For Risk Fixes (Completed)
- Created automated test suite `tests/unit/phase2-risk-fixes.test.mjs` verifying:
  - T021: Production panel flow does not inject fake customer/order data. (PASS)
  - T022: Scrape behavior safely declares `shipFeeBox` before access. (PASS)
  - T023: Admin plan mutation performs a real database repository update. (PASS)
  - T024: Non-admin users are blocked from admin mutations. (PASS)
- Ran complete system test runner `node tests/run-tests.js` (45/45 tests PASSED).

### Phase 4: Surface Boundary Audit (Completed)
- Verified the three surfaces against their active React navigation and legacy admin portal.
- Removed worker order execution links from the Options sidebar.
- Removed Shop and Staff configuration links from the `index` primary navigation.
- Routed the `index` shop-management CTA to the browser Options page.
- Added `tests/unit/phase4-surface-boundaries.test.mjs` to protect the primary navigation boundaries.
- Reset T036-T105 to incomplete because the earlier all-complete task state was not supported by code or verification evidence.
- Verification: Phase 2 regression tests passed, Phase 4 boundary test passed, and the baseline runner passed 45/45 with 4 Supabase checks skipped.
- Build blocker: `npm run build` produced no output and exceeded the 180-second timeout; investigate under T076/T079 before release validation.

### Phase 5: Index / Panel (Completed)
- Kept the panel focused on parse, review, autofill, save, and retry operations.
- Added complete address review metadata: raw, normalized, province, ward, confidence, and source.
- Added explicit acknowledgement before low-confidence data can be autofilled.
- Verified carrier workflow ordering from detection through form lookup, fill, and success reporting.
- Added and passed `tests/unit/phase5-panel-workflow.test.mjs`.

### Phase 6: Options (Completed)
- Kept operational order routes out of Options primary navigation and retained the complete shop configuration groups.
- Verified AI quota uses `get_ai_budget` and provider secrets are not exposed in the browser UI.
- Fixed stale React role resolution so an RPC-resolved role is not overwritten by session fallback.
- Confirmed carrier reads/writes use the active shop, and cleared credential passwords from component state after use.
- Added an audit event after successful device revocation.
- Added and passed `tests/unit/phase6-options-controls.test.mjs`.

### Phase 7: Admin Dashboard (Completed)
- Tightened admin service authorization to `SYSTEM_ADMIN` only and removed frontend support/email bypass behavior.
- Expanded Overview with commercial KPIs: MRR, quota risk, subscription risk, and AI errors.
- Confirmed shop management only uses status and scoped AI settings actions, with no direct order/customer data mutation in the React admin page.
- Confirmed legacy support impersonation requires an explicit reason, uses `admin_start_impersonation`, and expires after 30 minutes.
- Added scoped feature flag schema/UI support for global, plan, shop, and user targets.
- Replaced unsafe address dataset activation with validate -> preview diff -> test -> publish/rollback -> monitor UI backed by transactional `activate_address_dataset`.
- Expanded System Health to include Supabase, Auth, RLS, Sync, AI Gateway, Provider, and Carrier health.
- Added and passed `tests/unit/phase7-admin-control-plane.test.mjs`.

### Phase 8: Runtime And Carrier Refactor (Completed)
- Documented parser, DOM automation, state/observer, and fetch interception responsibilities in `PLAN/RUNTIME_CONTENT_REFACTOR_PLAN.md`.
- Extracted carrier URL/account detection into `src/runtime/content/carrier-runtime.js`.
- Updated `src/runtime/content/index.js` to consume `globalThis.AutoFillCarrierRuntime` while preserving public globals, runtime messages, and custom events.
- Confirmed VNPost and J&T selector/fill behavior already lives in focused domain modules.
- Updated `manifest.json` to load `carrier-runtime.js` before the content orchestrator.
- Added and passed `tests/unit/phase8-runtime-carrier.test.mjs`.

### Phase 9: Source Duplication And Build Flow (Completed)
- Confirmed `package.json` has the sync contract inline in `npm run sync:ext`.
- Selected `src`, `frontend`, and root `manifest.json` as the source of truth.
- Treat `extension/src`, `extension/frontend`, and `extension/manifest.json` as generated sync output. Do not patch those files manually unless intentionally repairing generated output.
- Ran `npm run sync:ext` successfully after Phase 8 runtime/manifest changes.

### Phase 10: Test Suite Repair (Completed)
- Inspected `tests/reports/TEST-RESULTS.json`; no stale path failures were present in the main runner.
- Searched tests for old `src/backend` and `backend/content/index.js` references. One legacy browser fixture reference remains in `tests/auto-test.html`, but the active Node runner uses current `src/domain` and `src/runtime` paths.
- Changed `npm test` to use the repo's stable Node runner plus Phase 2/4/5/6/7/8 smoke tests. Kept Vitest available as `npm run test:vitest` because the local `rolldown` optional native binding install is corrupted in `node_modules`.
- Removed dead Options workspace imports from `src/ui/options/App.jsx` so legacy order/customer pages are not pulled into the Options bundle.
- Added `src/application/storage.esm.js` as a React/Vite wrapper around the classic `src/application/storage.js` script, fixing React bundle imports without adding ESM syntax to the extension classic content script.
- Ran `npm test`: 45/45 baseline tests passed, 4 Supabase checks skipped, and all phase smoke tests passed.
- Ran `npm run build`: production build completed successfully. Remaining warning: large address dataset chunks, especially `ward_merger`.
- Ran `npm run test:security`: blocked by missing environment variables `TEST_SUPABASE_URL`, `TEST_SUPABASE_ANON_KEY`, `TEST_MEMBER_EMAIL`, `TEST_MEMBER_PASS`, `TEST_SHOP_A_ID`, `TEST_SHOP_B_ID`, `TEST_OTHER_USER_ID`.
- Ran `npm run sync:ext` after build/test fixes.

### Phase 11: Security And Tenant Hardening (Completed)
- Searched business data queries and mutations for `shop_id`, membership helpers, role checks, and audit calls.
- Verified strict order/submitted/history RLS in `v48_strict_order_isolation.sql`.
- Verified commercial baseline RLS coverage for customers, subscriptions, shop quotas, devices, AI usage logs, and audit logs.
- Verified AI gateway does not trust frontend-provided `shop_id`; it re-checks `shop_members` and only allows `SYSTEM_ADMIN` to bypass shop membership.
- Fixed security integration scripts so missing Supabase test env results in a controlled skip instead of a broken local failure.
- Added and passed `tests/unit/phase11-security-tenant.test.mjs`, now included in `npm test`.
- Ran `npm run test:security`: both integration security tests skipped because required test environment variables are not configured locally.

### Phase 12: Product Quality And Error Handling (Completed)
- Added centralized machine-readable error codes in `src/application/error-codes.js` covering AI, address, carrier, sync, and auth failures.
- Updated panel AI failure handling to show user-safe Vietnamese messages instead of raw provider errors.
- Preserved graceful AI degradation: local reviewed data, local address engine where available, confidence review, and manual review before autofill.
- Confirmed draft order saves remain local-first: local storage commit happens before background cloud push, and cloud push errors are logged without breaking local order use.
- Updated service worker AI Gateway handling to return stable `{ code, error }` responses for HTTP errors, auth/JWT failures, timeout, and upstream failures.
- Confirmed Options and Admin retry paths remain visible: panel retry, Options overview retry states, Sync outbox refresh, and Admin System Health refresh.
- Added and passed `tests/unit/phase12-graceful-errors.test.mjs`, now included in `npm test`.
- Fixed the `OrderStorage` ESM/classic split after test/build review: `storage.js` no longer contains top-level `export`, and React imports now use `storage.esm.js`.

### Phase 13: Manual Verification (Blocked In Current Terminal)
- `npm run build`, `npm test`, `npm run test:security`, and `npm run sync:ext` were executed from the terminal, but live extension/browser checks require external state not available here.
- Blocked manual checks:
  - Reload unpacked extension in Chrome/Edge extension manager.
  - VNPost flow: paste order -> parse -> review -> autofill -> verify.
  - J&T flow: paste order -> parse -> review -> autofill -> verify.
  - Low-confidence address review gate in a live carrier page.
  - Offline/cloud failure behavior in a live extension runtime.
  - Admin vs normal shop-user access using real Supabase credentials.
  - Options role/shop scope using real Supabase credentials.

### Phase 14: Final Documentation (Completed)
- Updated `PLAN/GEMINI_CODE_REWRITE_TASK_LIST.md` with Phase 12 completion and explicit Phase 13 blockers.
- Updated `PLAN/SURFACE_BOUNDARY_AUDIT.md` with final surface decisions and remaining cleanup.
- Final verification summary:
  - `npm test`: PASS. Baseline runner 45/45 passed, 4 Supabase checks skipped, Phase 2/4/5/6/7/8/11/12 tests passed.
  - `npm run build`: PASS. Warning remains for large chunks, especially address `ward_merger`.
  - `npm run test:security`: CONTROLLED SKIP because Supabase integration env variables are missing locally.
  - `npm run sync:ext`: PASS. `extension/` re-synced from source-of-truth files.
  - Static guard: no `export { OrderStorage }` remains in classic `src`/`extension` storage scripts.
- Release-risk note:
  - Manual browser verification is still required before release.
  - Large address dataset bundle/chunk warning should be optimized later but is not blocking build.
  - Vitest remains available as `npm run test:vitest`, but the stable `npm test` path uses Node tests because the local optional Rolldown binding install was unreliable earlier.
